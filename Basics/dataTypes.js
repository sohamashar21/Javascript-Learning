// NN BB SS U
let age = Number(18)
let food = null

let isLoggedIn = Boolean(false)
let big = BigInt(1234567)

let name = String("Soham")
let sym = Symbol("$")

let undefine = undefined;
console.log(age,food,isLoggedIn,big,name,sym)

// non primitive (objects)
let student ={
  age:19,
  name:"Soham ashar"
}

console.log(student["name"],student["age"]);
